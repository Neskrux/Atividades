1) Resposta:
Ao final do processamento, o valor da variável SOMA será 91.

2) Programa em Python para verificar se um número pertence à sequência de Fibonacci:
def verifica_fibonacci(numero):
    a, b = 0, 1
    while b < numero:
        a, b = b, a + b
    if b == numero:
        return f"{numero} pertence à sequência de Fibonacci."
    else:
        return f"{numero} não pertence à sequência de Fibonacci."

numero = int(input("Digite um número para verificar se pertence à sequência de Fibonacci: "))
print(verifica_fibonacci(numero))


3) Próximo elemento das sequências:
a) 9
b) 128
c) 49
d) 100
e) 13
f) 20

4) Resolução para descobrir qual interruptor controla cada lâmpada:

Ligue o primeiro interruptor e espere um tempo.
Desligue o primeiro interruptor e ligue o segundo interruptor.
Entre na sala:
Se houver uma lâmpada acesa, ela é controlada pelo segundo interruptor.
Se a lâmpada estiver apagada e quente, ela é controlada pelo primeiro interruptor.
Se a lâmpada estiver apagada e fria, ela é controlada pelo terceiro interruptor.
5) Programa em Python para inverter os caracteres de uma string:

def inverte_string(string):
    string_invertida = ''
    for i in range(len(string) - 1, -1, -1):
        string_invertida += string[i]
    return string_invertida

string = input("Digite uma string para inverter: ")
print("String invertida:", inverte_string(string))
